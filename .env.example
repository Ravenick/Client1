# Example env for this project — DO NOT commit secrets here.
# Populate local `.env` or set CI/Vercel environment variables instead.

# Public Vite client variables (safe to include)
VITE_SUPABASE_URL="https://eqbemskelwtstgvzzdsn.supabase.co"
VITE_SUPABASE_ANON_KEY="REPLACE_WITH_YOUR_VITE_SUPABASE_ANON_KEY"

# Server-side variables (DO NOT commit your service role key)
SUPABASE_URL="https://eqbemskelwtstgvzzdsn.supabase.co"
# Set `SUPABASE_KEY` in your deployment provider (Vercel/GitHub Actions)
SUPABASE_KEY="REPLACE_WITH_SERVICE_ROLE_KEY"

# Notes:
# - Keep a local `.env` with real secrets (ignored by git).
# - Rotate keys if secrets were previously leaked.
